package testsceipt_PWA;

import org.json.JSONObject;

import com.fasterxml.jackson.databind.ObjectMapper;

public class sample_compare {
	/*final JSONObject obj1 = {"RESPONSECODE":"1","ERRORCODE":"3","MESSAGE":"MEMBERID NOT AVAILABLE"};
	final JSONObject obj2 = json;
	final ObjectMapper mapper = new ObjectMapper();
	final JsonNode tree1 = ((Object) mapper).readTree(obj1.toString());
	final JsonNode tree2 = ((Object) mapper).readTree(obj2.toString());
	return tree1.equals(tree2);*/
}
